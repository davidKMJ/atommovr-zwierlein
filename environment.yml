name: atommovr_env
channels:
    - defaults
    - conda-forge
dependencies:
    - python=3.13
    - setuptools # needed because lazy-build calls PPSU/setupc.py
    - pip
    - pip:
          - -e ".[dev,hardware]"
